name: Release

on:
  push:
    tags:
      - 'v*.*.*'

permissions:
  contents: write

jobs:
  build:
    strategy:
      fail-fast: false
      matrix:
        include:
          - os: macos-15-intel
            flag: -m
          - os: macos-latest
            flag: -m
          - os: windows-2022
            flag: -w
          - os: ubuntu-latest
            flag: -l
    runs-on: ${{ matrix.os }}
    defaults:
      run:
        shell: bash

    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: '20.18.1'

      - name: Install dependencies
        run: |
          yarn set version 1.22.18
          yarn

      - name: Build and publish release
        env:
          CSC_IDENTITY_AUTO_DISCOVERY: false
          GH_TOKEN: ${{ github.token }}
        run: yarn build ${{ matrix.flag }} --publish always

      - name: Upload bundled artifacts
        uses: actions/upload-artifact@v4
        with:
          name: dist-${{ matrix.os }}
          path: dist_electron/bundled/*
          if-no-files-found: ignore

  homebrew:
    needs: build
    runs-on: ubuntu-latest
    steps:
      - name: Download macOS artifacts
        uses: actions/download-artifact@v4
        with:
          name: dist-macos-15-intel
          path: dist

      - name: Update Homebrew tap
        env:
          GH_TOKEN: ${{ secrets.HOMEBREW_TAP_TOKEN }}
        run: |
          VERSION="${GITHUB_REF_NAME#v}"
          DMG=$(ls dist/wemit-books-v*-mac-x64.dmg 2>/dev/null | head -1)
          if [ -z "$DMG" ]; then
            echo "No macOS DMG found — skipping Homebrew update"
            exit 0
          fi
          SHA256=$(sha256sum "$DMG" | cut -d' ' -f1)

          git clone https://x-access-token:${GH_TOKEN}@github.com/wemit/homebrew-books.git tap
          cd tap
          git config user.email "actions@github.com"
          git config user.name "github-actions"
          git checkout -b "release-${VERSION}"

          mkdir -p Casks
          {
            printf 'cask "wemit-books" do\n'
            printf '  version "%s"\n' "$VERSION"
            printf '  sha256 "%s"\n' "$SHA256"
            printf '\n'
            printf '  url "https://github.com/wemit/books/releases/download/v#{version}/wemit-books-v#{version}-mac-x64.dmg"\n'
            printf '  name "wemit-books"\n'
            printf '  desc "Offline desktop accounting with Estonian compliance"\n'
            printf '  homepage "https://github.com/wemit/books"\n'
            printf '\n'
            printf '  app "wemit-books.app"\n'
            printf 'end\n'
          } > Casks/wemit-books.rb

          git add Casks/wemit-books.rb
          git commit -m "books v${VERSION}"
          git push --force origin "release-${VERSION}"

          curl -s -X POST \
            -H "Authorization: Bearer ${GH_TOKEN}" \
            -H "Accept: application/vnd.github+json" \
            https://api.github.com/repos/wemit/homebrew-books/pulls \
            -d "{\"title\":\"books v${VERSION}\",\"head\":\"release-${VERSION}\",\"base\":\"main\",\"body\":\"Automated cask update for v${VERSION}.\"}"

  scoop:
    needs: build
    runs-on: ubuntu-latest
    steps:
      - name: Download Windows artifacts
        uses: actions/download-artifact@v4
        with:
          name: dist-windows-2022
          path: dist

      - name: Update Scoop bucket
        env:
          GH_TOKEN: ${{ secrets.SCOOP_BUCKET_TOKEN }}
        run: |
          VERSION="${GITHUB_REF_NAME#v}"
          EXE=$(ls dist/wemit-books-v*-windows-x64.exe 2>/dev/null | head -1)
          if [ -z "$EXE" ]; then
            echo "No Windows installer found — skipping Scoop update"
            exit 0
          fi
          SHA256=$(sha256sum "$EXE" | cut -d' ' -f1)

          git clone https://x-access-token:${GH_TOKEN}@github.com/wemit/scoop-books.git bucket
          cd bucket
          git config user.email "actions@github.com"
          git config user.name "github-actions"
          git checkout -b "release-${VERSION}"

          mkdir -p bucket
          jq -n \
            --arg v "$VERSION" \
            --arg h "sha256:$SHA256" \
            '{
              version: $v,
              description: "Offline desktop accounting with Estonian compliance",
              homepage: "https://github.com/wemit/books",
              license: "AGPL-3.0",
              url: ("https://github.com/wemit/books/releases/download/v" + $v + "/wemit-books-v" + $v + "-windows-x64.exe"),
              hash: $h,
              installer: {
                script: ("Start-Process -Wait \"$dir\\wemit-books-v" + $v + "-windows-x64.exe\" -ArgumentList \"/S\"")
              }
            }' > bucket/books.json

          git add bucket/books.json
          git commit -m "books v${VERSION}"
          git push --force origin "release-${VERSION}"

          curl -s -X POST \
            -H "Authorization: Bearer ${GH_TOKEN}" \
            -H "Accept: application/vnd.github+json" \
            https://api.github.com/repos/wemit/scoop-books/pulls \
            -d "{\"title\":\"books v${VERSION}\",\"head\":\"release-${VERSION}\",\"base\":\"main\",\"body\":\"Automated manifest update for v${VERSION}.\"}"
